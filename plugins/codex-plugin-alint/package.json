{
  "name": "@alint-js/codex-plugin",
  "type": "module",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "tsdown --config tsdown.config.ts && tsdown --config tsdown.launcher.config.ts",
    "test": "pnpm build && vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@alint-js/utils": "workspace:*",
    "@moeru/std": "catalog:",
    "tinyexec": "catalog:"
  }
}
